{
  "name": "eco-npm",
  "version": "1.0.0",
  "description": "Query the npm database",
  "main": "src/index.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start:dev": "nodemon",
    "start": "node build/index.js",
    "test": "jest",
    "test:dev": "jest --watchAll",
    "lookup": "node --loader ts-node/esm src/test.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.15",
    "prettier": "^2.5.1",
    "rimraf": "^3.0.0"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "express": "^4.17.2",
    "semver": "^7.3.5"
  }
}
